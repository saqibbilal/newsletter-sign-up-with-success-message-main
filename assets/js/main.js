const form = document.querySelector("form");
form.addEventListener("submit", (e) => {
    e.preventDefault();


    // if valid{
        // submit form
        // hideform
        // Show success

    // }
    // else{
        // validation error
// }
})

function switchPage(){
    
}